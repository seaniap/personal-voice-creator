# Build Log — How `personal-voice-creator` Was Made

> 這份 log 記錄真實的製作過程，包含卡住、出錯、修正方向的時刻。
> 它是寫 Medium 文章的初稿骨架——重點不是流水帳，而是那些「失敗 → 調整」的真實片段。

---

## 起點：一個關於手機前主人的問題

整件事的開頭其實有點偶然。我先讓 Claude 從它對我的記憶裡，描述「如果我是這支手機的新主人，前一個主人是什麼樣的人」。它講出來的版本——橫跨技術與人文、選「蓄」當年度漢字、做事有結構但不死板——意外地準。

那一刻的念頭是：既然它這麼懂我的輪廓，能不能把「我這個人」固化成一個工具，讓它每次寫東西都像我寫的？

---

## 階段一：訪談，把「我」文字化

沒有一開始就寫檔案，而是先做了一段訪談。Claude 一次問一個問題：

- 你最不想讓讀者有什麼感覺？→ 我說：不想顯得在賣弄、在背稿、依老賣老
- 結構先行還是跟著感覺走？→ 偏 A（結構），偶爾 B
- 對不同對象語氣會變嗎？→ 會：社群朋友要自然、客戶要有 CEO 份量、日本朋友要用 N2 程度的日語
- 有什麼句型習慣？→ 金字塔結構、愛舉小例子、偶爾夾英日文、複雜的東西喜歡畫圖說明

**心得：** 一次問一題比一次丟一堆問題有用太多。前者像對話，後者像填表，而填表的時候人會給出「理想中的自己」，不是真實的自己。

---

## 階段二：真實樣本 vs. 自我描述的落差

接著我貼了五篇自己的 Medium 文章。Claude 讀完後抓出來的特徵，跟我自己講的有些對得上，有些是我沒意識到的——例如我在技術文章裡習慣「先給整體架構圖再拆解」、在非技術文裡「先引英文定義再用生活比喻解釋」。

**關鍵教訓：真實樣本比自我描述可靠。** 人形容自己時會美化或漏掉習慣，但寫過的東西不會說謊。

---

## 階段三：測試與校準——這裡才是真正的價值

Claude 寫了一段「我為什麼開始認真學日語」的社群短文。第一版我改了四個地方：

| 原句 | 我的修正 | 為什麼 |
|------|----------|--------|
| 「說起來有點難為情」 | 刪掉，直接進主題 | 我很少用這種自謙開場 |
| 「我不是要去日本工作」 | 「我並不是要去日本工作」 | 否定句我習慣更肯定的語氣 |
| 「看不見的玻璃」 | 「看不見的牆」 | 選字要更有力道 |
| 「不是用翻譯工具糊弄過去，是真的消失」 | 「那種感覺，是翻譯工具給不了的」 | 原句邏輯跳躍，主詞混亂 |

然後是商務信。第一版 Claude 寫得**太像在跟一般朋友聊天**——「最近跟幾位行銷主管聊下來」「找個時間跟您聊聊」。重寫後改成「近期與幾位行銷主管交流」「歡迎進一步交流」，才有了該有的份量感。

**最大的體會：校準時的每一個修正，才是這個工具真正的產出。** 它捕捉的是「我以為我怎麼寫」和「我實際怎麼寫」之間的那道縫。後來我把這些修正全部存進 `calibration-notes.md`。

---

## 階段四：上傳真實信件，語氣才真正到位

我把整理好的「客戶與廠商信件內容整理」上傳。Claude 從裡面歸納出我真實的書信規則：不同對象的稱謂（女士／老師／Hi ○○／Dear）、不同正式程度的落款（順頌商祺／祝順心／謝謝）、用編號清單列選項、用括號補充。這一步讓商務模式從「像」變成「對」。

---

## 階段五：打包，連續踩了兩次坑

打包成 `.skill` 時遇到第一個技術錯誤：

```
ModuleNotFoundError: No module named 'scripts'
```

原因是打包腳本 `package_skill.py` 會去 import 同目錄的 `quick_validate.py`，但我只複製了單一檔案。第一次修：把整個 `scripts/` 目錄複製過來——成功。

但這帶出一個設計問題：**如果這個工具要給別人用，別人沒有那一整包依賴怎麼辦？** 所以做 creator 版本時，特地把打包腳本改寫成「自帶驗證、零外部依賴」的獨立版本，只靠一個 PyYAML。這是從自己踩的坑回頭優化別人的體驗。

---

## 階段六：從「一個 skill」到「一個能生 skill 的 skill」

最後的轉折，是決定把這整個過程本身做成一個工具——`personal-voice-creator`。它把上面的訪談、建檔、測試校準、打包流程「制度化」，讓任何人都能跑一遍，產出屬於自己的 `personal-voice.skill`。

關於開發方式，也做了幾個刻意的取捨：
- **不寫完整 SDD**，改用 Claude Code 原生的 `CLAUDE.md`——因為這個專案的本體只是幾個 Markdown 加一支腳本，正式規格文件是用大砲打小鳥，違反 KISS。
- **CI 只做一件有意義的事**：每次 PR 自動跑打包驗證，確認沒人不小心破壞 skill 結構（例如多塞一個 SKILL.md 會導致上傳失敗）。
- **Issues 開著**，當作未來「@claude 自動維護」的入口。

---

## 回頭看，學到的三件事

1. **一次問一題。** 對話式訪談逼出真實，填表式問卷只拿到理想版本。
2. **樣本勝過自述。** 真實寫過的東西，比任何自我形容都準。
3. **修正即產品。** 校準迴圈裡的每一句「我不會這樣說」，才是把 AI 代筆變成「像我」的關鍵。

---

> *接下來在 Claude Code 持續開發時，每有值得記的轉折就往下補一條。*

### 後續更新
- _(日期 / 改了什麼 / 為什麼)_
